# FreeBSD platform compatibility audit

Reviewed: 2026-09-20

This inventory covers every Rust source line matched by the command below. The five release binaries are `mur`, `mur-mcp-server`, `murmurd`, `mur-agent-runtime`, and `mur-research-gateway`. GUI-only crates are recorded because the scan is repository-wide, but they are not reachable from those five artifacts.

## Verification

```sh
git grep -nE 'target_os|target_family|cfg!\(|std::env::consts::OS|systemd|launchd|notify-send|osascript' -- '*.rs' > /tmp/freebsd-platform-hits.txt
python3 scripts/check-freebsd-audit.py
```

The checker recomputes the same matches and fails for either missing or stale rows. Rows are keyed by file plus the matched source text (whitespace-trimmed), not by line number, so edits that only shift lines do not need an audit update. A file with several identical matched lines needs one row per occurrence. In the source column, `|` is written as `\|`, and code containing backticks uses a longer fence.

## Inventory

| file | matched source | binary reachability | classification | required change | task |
|---|---|---|---|---|---|
| `mur-agent-gui/src-tauri/src/bootstrap.rs` | `p.join(if cfg!(windows) {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `// (kill, launchd shutdown, parent process death), make sure` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `` // itself is `#[cfg(any(target_os = "macos", target_os = `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/main.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/decode.rs` | `d.join(if cfg!(windows) {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/heic.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/heic.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/heic.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/heic.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/mod.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/platform.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/platform.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/tesseract.rs` | `#![cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/multimodal/ocr/vision.rs` | `#![cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/self_test.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/dock.rs` | `#![cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/services.rs` | `#![cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/send/services_provider.rs` | `#![cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/sidecar.rs` | `if cfg!(target_os = "macos") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/sidecar.rs` | `} else if cfg!(target_os = "linux") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/sidecar.rs` | `if cfg!(target_os = "windows") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/sidecar.rs` | `if cfg!(target_os = "macos") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/test_harness.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/test_harness.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/voice/hotkey.rs` | `let mods = if cfg!(target_os = "macos") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/src/voice/stt/whisper.rs` | `` //! feature on `cfg(target_os = "macos")`); Linux/Windows fall back to `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_heic.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_heic.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_heic.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_pipeline_image.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_pipeline_image.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/multimodal_pipeline_image.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_dock.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_services.rs` | `` //! The whole channel is gated `#[cfg(target_os = "macos")]` because it `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-gui/src-tauri/tests/send_services.rs` | `#![cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-agent-runtime/src/exec_dirs.rs` | `/// launched by launchd/systemd rather than an interactive shell.` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0.rs` | `// lives since #935 — and launchd/systemd hand the runtime a PATH` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `#[cfg_attr(not(target_os = "windows"), allow(dead_code))]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `if cfg!(windows) {` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/hooks/b0_helpers.rs` | `#[cfg(all(not(target_os = "macos"), not(target_os = "windows")))]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/mcp_repin.rs` | `` //! `research-gateway` changed since install`. launchd/systemd restarts it, it `` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/oauth/mod.rs` | `if let Ok(child) = std::process::Command::new(if cfg!(target_os = "macos") {` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/protocol/mcp_client.rs` | `// the standard install dirs a GUI/launchd parent omits — so which` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/sandbox/child.rs` | `#[cfg(any(target_os = "linux", target_os = "macos"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/child.rs` | `#[cfg(not(any(target_os = "linux", target_os = "macos")))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/launch_chain.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/launch_chain.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/launch_chain.rs` | `home.join(".config/systemd/user"),` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/launch_chain.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `let arch = if cfg!(target_arch = "x86_64") {` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/linux.rs` | `} else if cfg!(target_arch = "aarch64") {` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/mod.rs` | `#[cfg(not(any(target_os = "linux", target_os = "macos", target_os = "windows")))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(target_os = "windows")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `` // `mut` is only used inside the #[cfg(target_os = "macos")] block below; `` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/sandbox/policy.rs` | `#[cfg(not(target_os = "windows"))]` | `mur-agent-runtime` sandbox path | `explicit-freebsd` | Use the existing non-Linux/macOS/Windows fallback; native CI must compile and test it. | Task 6 |
| `mur-agent-runtime/src/tools/bash.rs` | `` /// (launchd/systemd, e.g. `/usr/bin:/bin:/usr/sbin:/sbin`) — without it, `` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/transport/unix_socket.rs` | `#[cfg(target_os = "linux")]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/transport/unix_socket.rs` | `#[cfg(target_os = "macos")]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/src/transport/unix_socket.rs` | `#[cfg(not(any(target_os = "linux", target_os = "macos")))]` | `mur-agent-runtime` runtime path | `portable` | No source change identified; native workspace tests verify the generic Unix path. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_mcp_signature.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_mcp_signature.rs` | `#[cfg(target_os = "linux")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_mcp_signature.rs` | `#[cfg(target_os = "windows")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_signability.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_signability.rs` | `let magic: &[u8] = if cfg!(windows) {` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_signability.rs` | `#[cfg(any(target_os = "macos", target_os = "windows"))]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b0_rule11_signability.rs` | `#[cfg(not(any(target_os = "macos", target_os = "windows")))]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/b1_spawn_allowlist_enforce.rs` | `#![cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_build_lane.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_build_lane.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_e2e.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_e2e.rs` | `#[cfg(target_os = "windows")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_e2e.rs` | `#[cfg_attr(target_os = "macos", ignore)]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-agent-runtime/tests/sandbox_e2e.rs` | `#[cfg(target_os = "linux")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-common/src/agent.rs` | `` /// `AgentProfile.locale`: under launchd there is no `LANG`, so the old `` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/binary_attestation.rs` | `if !IS_EMBEDDED_RELEASE \|\| !cfg!(target_os = "macos") {` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/deps/detect.rs` | `let exts: &[&str] = if cfg!(windows) {` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/deps/mod.rs` | `format!("{}-{}", std::env::consts::ARCH, std::env::consts::OS)` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/exec.rs` | `#[cfg(target_os = "windows")]` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/exec.rs` | `/// The ambient PATH plus the well-known install dirs that GUI/launchd parents` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/local_llm.rs` | `//! OpenAI-compatible base URL here. Agents — started by launchd and therefore` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-common/src/schedule.rs` | `/// System cron/launchd (mur CLI solo mode)` | shared by release binaries | `portable` | No source change identified; platform APIs or generic Unix behavior already cover FreeBSD. | Task 6 |
| `mur-core/src/agent_wizard/apply.rs` | `// 2. Ensure model_ref resolves under launchd.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/auth.rs` | `std::env::consts::OS.to_string()` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/auth.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/auth.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/auth.rs` | `#[cfg(target_os = "windows")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cli/agent.rs` | `/// Start a stopped agent: via its launchd/systemd service when one is` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cli/agent.rs` | `/// Stop a running agent and keep it stopped: unloads its launchd/systemd` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cli/agent.rs` | `/// for exit, then polls for the launchd-respawned process.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cli/agent.rs` | `/// Remove an agent: deletes its runtime symlink and its launchd/systemd` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cli/agent.rs` | `/// Generate and (optionally) install a launchd/systemd user service` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/app/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/app/mod.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/input.rs` | `` /// ponytail: macOS-only via `osascript` (zero new deps — the clipboard already `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/input.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/input.rs` | `let out = std::process::Command::new("osascript")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/input.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login.rs` | `` // non-test consumer is `keychain_stamp`, which is `#[cfg(target_os = "macos")]`. `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login.rs` | `all(not(target_os = "macos"), not(test)),` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login.rs` | `Self::detect_with(cfg!(target_os = "macos"), \|name\| {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login/stamp.rs` | `all(not(target_os = "macos"), not(test)),` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login/stamp.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login/stamp.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/login/tests.rs` | `let (bin, args): (&str, &[&str]) = if cfg!(windows) {` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `` /// The macOS `osascript` line for a notification, with quotes escaped. Pure so `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `#[cfg_attr(not(target_os = "macos"), allow(dead_code))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `let _ = std::process::Command::new("osascript")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `let _ = std::process::Command::new("notify-send")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/notify.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/panel.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/cli/panel.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/hub.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/hub.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/lifecycle.rs` | `let canonical = bin_dir.join(if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/lifecycle.rs` | `` // launchd job with `RunAtLoad` recreates the directory and the warning `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/lifecycle.rs` | `// launchd/systemd restart the runtime the instant it dies (every service` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/lifecycle.rs` | `` // being down: a launchd job with KeepAlive (every service `mur agent `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/lifecycle.rs` | `#[cfg(any(target_os = "macos", target_os = "linux"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/mod.rs` | `let runtime_filename = if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/model_resolve.rs` | `let apple_silicon = cfg!(all(target_os = "macos", target_arch = "aarch64"));` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart.rs` | `` //! (launchd `KeepAlive=true` respawns on exit). Lock is NOT pre-removed — `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `//! launchd needs the process to exit, not the lock to disappear.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `// Whether a launchd/systemd service unit is installed for this agent —` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `// launchd KeepAlive=true respawns on process exit. Confirmation lives in` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `// kickstart can only kick a job launchd is tracking. After a manual` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `` /// - Linux: `$XDG_CONFIG_HOME/systemd/user/mur-agent-<name>.service` `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `Some(dirs::config_dir()?.join(format!("systemd/user/mur-agent-{name}.service")))` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `/// Passive wait for launchd/systemd's natural respawn. Covers respawn latency` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `/// real launchd respawn can take ~2 min after rapid restarts.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `// Passive: launchd KeepAlive=true respawns on process exit; wait for the` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart_confirm.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/restart.rs` | `/// launchd's KeepAlive for the same lock.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/restart.rs` | `/// spawns a second runtime next to one launchd is already restarting.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` //! `mur agent install-service` — generate launchd / systemd unit files, and `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` /// config dir on Linux (`$XDG_CONFIG_HOME/systemd/user/...`), matching what `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(any(target_os = "macos", target_os = "linux"))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `base.join(format!("systemd/user/mur-agent-{name}.service"))` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(any(target_os = "macos", target_os = "linux"))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(not(target_os = "linux"))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// on purpose: disabling is a launchd/systemd-level statement about the job,` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(any(target_os = "macos", target_os = "linux"))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `// rewrote the plist on disk and launchd went right on using the` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `println!("Installed launchd service at {}", dest.display());` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` // Same reason as the launchd leg above: `daemon-reload` makes systemd `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `println!("Installed systemd --user unit at {}", dest.display());` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `bail!("install-service only supports macOS (launchd) and Linux (systemd --user)")` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// Directories launchd hands every process by default (its minimal PATH),` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// binaries (npm/homebrew) resolve when launchd/systemd starts the agent` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` /// absent or fails), `/opt/homebrew/bin`, `/usr/local/bin`, then the launchd `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// launchd's nor systemd's. Without it a sibling binary the agent pins --` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// interactively and fail-closes under launchd, crash-looping the agent.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `/// Where the launchd unit sends the runtime's stderr — single source for the` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `fn derive_service_path_always_has_launchd_defaults() {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` /// stale `/opt/homebrew/bin` copy under launchd while an interactive start `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/service.rs` | `` /// did: absent made `stop` skip the bootout, so launchd's KeepAlive brought `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/skill_github.rs` | `("launchctl", "launchd persistence"),` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stale.rs` | `let canonical = bin_dir.join(if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stale.rs` | `let canonical = bin_dir.join(if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stale.rs` | `let canonical = bin_dir.join(if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/start.rs` | `//! 2. a service unit exists (launchd plist / systemd --user unit) → start it` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `//! through the service manager so supervision stays with launchd/systemd;` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `// no fallback here, or we would attest a binary launchd will not` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `println!("started '{name}' via launchd ({label})");` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `println!("loaded + started '{name}' via launchd ({label})");` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `"launchd refused to start '{name}': kickstart: {} / load: {}",` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `.join(format!("systemd/user/mur-agent-{name}.service"));` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `// Same reasoning as the launchd leg above.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `"systemd refused to start '{name}': {}",` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `println!("started '{name}' via systemd --user");` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `#[cfg(any(target_os = "macos", target_os = "linux"))]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/start.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/agent/stats.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `` /// The systemd unit sets no `StandardError=`, so the runtime's stderr goes to `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `if cfg!(target_os = "linux") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `` /// The bug: `mur agent start` writes `<agent>/stderr.log`, the launchd unit `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `/// Non-macOS has no second file at all (systemd logs to the journal), so` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent/stats.rs` | `assert_eq!(n, if cfg!(target_os = "macos") { 2 } else { 1 });` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `.join(if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `if cfg!(windows) { ".exe" } else { "" }` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `let bundles = match std::env::consts::OS {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `if cfg!(not(target_os = "macos")) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `if cfg!(not(target_os = "macos")) \|\| opts.skip_notarize {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `if cfg!(not(target_os = "macos")) \|\| opts.skip_notarize {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `if cfg!(not(target_os = "macos")) \|\| opts.skip_notarize {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `let (subdir, ext): (&str, &str) = match std::env::consts::OS {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/agent_export_gui.rs` | `let triple = match (std::env::consts::OS, std::env::consts::ARCH) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-browser/src/chromium.rs` | `if cfg!(target_os = "macos") {` | `mur` CLI path | `portable` | No source change identified; FreeBSD falls through to the generic Unix `XDG_CACHE_HOME`/`~/.cache/ms-playwright` default, matching Linux. | Task 6 |
| `mur-browser/src/chromium.rs` | `if cfg!(windows) {` | `mur` CLI path | `portable` | No source change identified; FreeBSD falls through to the generic Unix `XDG_CACHE_HOME`/`~/.cache/ms-playwright` default, matching Linux. | Task 6 |
| `mur-core/src/cmd/browser/doctor.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/src/cmd/browser/doctor.rs` | `let name = if cfg!(windows) { "npx.exe" } else { "npx" };` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/src/cmd/browser/setup.rs` | `let name = if cfg!(windows) { "npx.exe" } else { "npx" };` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/browser/mod.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/deep_research/ask.rs` | `// (Hub-launched concierge: /opt/homebrew/bin first; launchd worker:` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `let triple = match (std::env::consts::OS, std::env::consts::ARCH) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `#[cfg(target_os = "windows")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `format!("unrecognised platform: {}", std::env::consts::OS),` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `if cfg!(target_os = "macos") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `} else if cfg!(target_os = "windows") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `cfg!(target_os = "linux"),` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/doctor.rs` | `if cfg!(target_os = "linux") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/init_daemon.rs` | `//! Installs murmurd as a login-persistent service (launchd / systemd).` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `install_launchd(murmurd_path)?;` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `install_systemd(murmurd_path)?;` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `#[cfg(target_os = "macos")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `fn install_launchd(murmurd_path: &Path) -> Result<()> {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `#[cfg(target_os = "linux")]` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `fn install_systemd(murmurd_path: &Path) -> Result<()> {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `.join("systemd")` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_daemon.rs` | `// Enable + start (ignore errors — systemd may not be running, e.g. in containers)` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/init_local.rs` | `let apple_silicon = cfg!(target_os = "macos") && cfg!(target_arch = "aarch64");` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/media/resolve.rs` | `/// Fallback for GUI-launched processes (Hub sidecar, launchd services): their` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/misc.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/misc.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/misc.rs` | `/// app. A foreground run can re-prompt; a Hub- or launchd-spawned agent cannot,` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `/// Send a desktop notification. macOS uses osascript; Linux uses notify-send if available.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `let _ = std::process::Command::new("osascript")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `// Try notify-send on Linux` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/project/mod.rs` | `let _ = std::process::Command::new("notify-send")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `let cmd = if cfg!(target_os = "macos") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `} else if cfg!(target_os = "windows") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `if cfg!(target_os = "windows") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/session.rs` | `#[cfg(target_os = "windows")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `/// Generate launchd / systemd unit files for scheduled sync.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `.join("systemd/user");` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/source_cmd.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/system_schedule.rs` | `//! System-level schedule integration (launchd on macOS, crontab on Linux).` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `` /// launchd and cron start with a bare `/usr/bin:/bin:/usr/sbin:/sbin`, so a `` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `if cfg!(target_os = "macos") {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `install_launchd(workflow_name, cron_expr)` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `if cfg!(target_os = "macos") {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `remove_launchd(workflow_name)` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `if cfg!(target_os = "macos") {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `list_launchd()` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `// ── macOS launchd ──────────────────────────────────────────────────` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `/// Convert a 5-field cron expression to launchd calendar interval.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `/// Wildcards (*) are omitted (launchd treats missing fields as "any").` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `fn install_launchd(workflow_name: &str, cron_expr: &str) -> Result<()> {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `fn remove_launchd(workflow_name: &str) -> Result<()> {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `fn list_launchd() -> Vec<(String, String)> {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `results.push((wf_name, "launchd".to_string()));` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `if cfg!(target_os = "macos") {` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `list_launchd()` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/system_schedule.rs` | `// What install_launchd writes for "30 9 * * 1-5" — only Minute/Hour/Weekday present.` | `mur` agent/service command | `unsupported` | Retain the explicit non-macOS/Linux fallback and document the unsupported service integration. | Task 9 |
| `mur-core/src/cmd/workflow.rs` | `/// or launchd fire has no way to obtain.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/workflow.rs` | `// label: launchd and crontab both read the cron expression as local` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/workflow.rs` | `// Both executors interpret the cron expression in LOCAL time: launchd's` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/workflow.rs` | `// Install system-level scheduler (launchd on macOS, crontab on Linux)` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/workflow.rs` | `if cfg!(target_os = "macos") {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/cmd/workflow.rs` | `" launchd plist installed at ~/Library/LaunchAgents/com.mur.schedule.{}.plist",` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/model_setup/slots.rs` | `if cfg!(test) {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `/// body still ends up as a single osascript line. Named rather than` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `` /// Escapes `s` for embedding inside an osascript double-quoted literal, `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `/// osascript line unescaped. Backslashes are escaped before quotes so` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `/// One osascript line, with quotes, backslashes and newlines escaped.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `std::process::Command::new("osascript")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `.map_err(\|e\| format!("osascript: {e}"))` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `std::process::Command::new("notify-send")` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `.map_err(\|e\| format!("notify-send: {e}"))` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `fn quotes_and_backslashes_are_escaped_for_osascript() {` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/monitor/notify/desktop.rs` | `// the osascript line and silently deliver half a message.` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/cow.rs` | `#[cfg(not(target_os = "macos"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/detect.rs` | `#[cfg(any(target_os = "linux", target_os = "freebsd"))]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/parallel/backend/detect.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/schedule_status.rs` | `` //! - OS-level workflow schedules (launchd / crontab, via `cmd::system_schedule`) `` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/server/mod.rs` | `#[cfg(target_os = "macos")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/server/mod.rs` | `#[cfg(target_os = "linux")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/server/mod.rs` | `#[cfg(target_os = "windows")]` | `mur` CLI path | `portable` | No source change identified; native workspace tests verify the existing fallback. | Task 6 |
| `mur-core/src/update/mod.rs` | `os = std::env::consts::OS,` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/mod.rs` | `let tmp_bin = tmp_dir.path().join(if cfg!(windows) {` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/mod.rs` | `if cfg!(windows) { "mur.exe" } else { "mur" },` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/mod.rs` | `// the launchd copy is refreshed from THIS release, not from whatever` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/mod.rs` | `/// that one, because it also has to reach the launchd copy and the brew keg,` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/source.rs` | `#[cfg(target_os = "freebsd")]` | `mur` update path | `explicit-freebsd` | FreeBSD branch of install-source detection; covered by host-independent tests. | Task 3 |
| `mur-core/src/update/source.rs` | `#[cfg(not(target_os = "freebsd"))]` | `mur` update path | `explicit-freebsd` | Non-FreeBSD branch of install-source detection; covered by host-independent tests. | Task 3 |
| `mur-core/src/update/release.rs` | `asset_name_for(std::env::consts::OS, std::env::consts::ARCH)` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `//! each binary as a brand-new application: grants die, and Hub/launchd-spawned` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg_attr(not(target_os = "macos"), allow(dead_code))]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `/// launchd copy (installed siblings can lag the self-update).` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg_attr(not(target_os = "macos"), allow(unused_variables))]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg(target_os = "macos")]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg_attr(not(target_os = "macos"), allow(dead_code))]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg_attr(not(target_os = "macos"), allow(dead_code))]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `#[cfg(target_os = "macos")]` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `// Refresh the launchd runtime copy BEFORE the identity gate: shipping` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `` /// dir (`mur-agent-runtime` lives there unlinked), and the launchd COPY at `` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/src/update/resign.rs` | `/// The launchd services run a COPY (not a symlink) of the runtime at` | `mur` update path | `explicit-freebsd` | Add or verify FreeBSD asset/pkg behavior with host-independent tests. | Task 3 |
| `mur-core/tests/agent_card_ephemeral.rs` | `let candidate = mur.parent()?.join(if cfg!(windows) {` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/tests/agent_install_service.rs` | `// Windows: gated — install-service emits launchd / systemd files only;` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/tests/agent_install_service.rs` | `#[cfg(target_os = "macos")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/tests/agent_install_service.rs` | `#[cfg(target_os = "linux")]` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/tests/agent_start_without_symlink.rs` | `` //! fallback and refused outright; `install-service` then wrote a launchd plist `` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-core/tests/gate_golden.rs` | `"deploy the murmurd binary to production and install the launchd plist",` | test-only | `portable` | Keep compiling/running under the native FreeBSD workspace test lane. | Task 6 |
| `mur-gui-core/src/autostart/linux.rs` | `//! Linux autostart via systemd --user units (spec §3.1).` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/linux.rs` | `` //! Unit: `~/.config/systemd/user/run.mur.agent.<slug>.service` `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/linux.rs` | `std::fs::create_dir_all(parent).context("create systemd user dir")?;` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/linux.rs` | `std::fs::write(&unit_path, &unit).context("write systemd unit")?;` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/linux.rs` | `.join("systemd")` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `//! macOS autostart via launchd LaunchAgents (spec §3.1, §3.5).` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `/// Build the launchd plist contents for an agent. Pure (no I/O) so it is unit-testable.` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `` /// Sets `MUR_HOME` in the agent's environment so the launchd-spawned runtime resolves `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `` // 401s as `x-api-key`). launchd agents are long-lived, so we inject when the `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `std::fs::write(&plist_path, &plist).context("write launchd plist")?;` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `std::fs::remove_file(&plist_path).context("remove launchd plist")?;` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `// kill TERM → launchd will restart if KeepAlive=true, but stop_service` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/macos.rs` | `// The launchd-spawned runtime must inherit MUR_HOME so it resolves the` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/autostart/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/ipc/mod.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `//! owns its lifetime — no launchd / systemd. This guarantees exactly one` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `//! (launchd-based always-on background services remain available separately via` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `let mut c = Command::new(if cfg!(windows) { "cmd" } else { "true" })` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `/// A runtime the Hub did not spawn (CLI, launchd, an earlier Hub) has to` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/sidecar.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(not(any(target_os = "macos", target_os = "windows", target_os = "linux")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/stub/mod.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/voice/dnd.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/voice/dnd.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/voice/dnd.rs` | `#[cfg(not(any(target_os = "macos", target_os = "windows")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/voice/dnd.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-gui-core/src/voice/dnd.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chat_window.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chat_window.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/app_server.rs` | `/// Hub's own PATH — launchd hands GUI apps a bare` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `if cfg!(target_os = "macos") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `} else if cfg!(target_os = "linux") {` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `Some(dirs::config_dir()?.join("systemd/user/mur-model-gateway.service"))` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `#[cfg(target_os = "linux")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/chatgpt_subscription/process.rs` | `#[cfg(not(any(target_os = "macos", target_os = "linux")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/cli_tools.rs` | `/// app runs it, because launchd hands GUI apps a bare` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/companion_notify.rs` | `` //! A MUR agent cannot post a macOS notification. Its `osascript` is refused by `` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/companion_notify.rs` | `//! — a launchd-started agent has no bundle identity of its own to be granted.` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/detail_window.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/import_muragent.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/import_muragent.rs` | `#[cfg(not(target_os = "windows"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/lib.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/lib.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/lib.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/lib.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/macos_un.rs` | `#[cfg(all(target_os = "macos", feature = "macos-un-notifications"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/macos_un.rs` | `#[cfg(not(all(target_os = "macos", feature = "macos-un-notifications")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/mcp_skills.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/mcp_skills.rs` | `#[cfg(target_os = "windows")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/mcp_skills.rs` | `#[cfg(all(unix, not(target_os = "macos")))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/mlx_sidecar.rs` | `//! publishes its base URL via the shared file so launchd-managed agents can` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/onboarding/first_launch.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/onboarding/first_launch.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/onboarding/first_launch.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/onboarding/first_launch.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/onboarding/first_launch.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/panel/pos.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/panel/pos.rs` | `#[cfg(not(target_os = "macos"))]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
| `mur-hub-gui/src-tauri/src/panel/pos.rs` | `#[cfg(target_os = "macos")]` | not reachable (GUI-only crate) | `unsupported` | No release-binary change; FreeBSD GUI is outside this support scope. | Task 9 |
